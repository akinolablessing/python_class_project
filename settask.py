from tkinter.font import names
from tokenize import String
from typing import Any


#def cipher(value):
   # value1 = "Hello, World!"
    #value2 = "Uryyb, jbeyq!"
   # value1.split("Hello, World!")
   # change = int(value1)
    #for count in change:
      #  if count > 26:
          #  count = count - 26
           # return count
        #else:
          #  break
           # return null

#def arrayList():
   # def_init_(self):
        #self._size = 0
        #self._element = [None] * 10
#def add(self, name: str) -> bool:
   # if name is None or len(name) == 0:
      #  return False


#for count in range(10):
    #if count % 2 == 0:
        #if count % 3 == 0:

#def task(name):
  #  name = "ayo"

   # return "ayo"
#def change_char_values(char: Any)->str:

#def change_char_values(char1:str)->str:
    #if 'a' <= char1 <= 'z':
       # return chr(((ord(char1)-ord('a') + 13)% 26) + ord('a'))
    #elif 'A' <= char1 <= 'Z':
        #return chr(((ord(char1)-ord('A')+13 )% 26) + ord('A'))

    #for char in char1:
       # if char == ',' or char == '.' or char == ':' or char == '?' or char == '!' or char == '?':
            #return char
        #else:
           # return change_char_values(char)

def sort_numbers(value:list) -> list:
    value.sort()
    return value


def sort_letter(elements:list) -> list:
    elements.sort()
    return elements